import { Typography } from "@phantomthief-react/components";
import styled from "styled-components";

export const Styled = {
  ExternalLink: styled(Typography)`
    display: flex;
    gap: 10px;
  `,
};
